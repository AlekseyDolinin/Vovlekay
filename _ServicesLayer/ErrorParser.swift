import Foundation

class ErrorParser {
    
    class func parse(input: Response_) {
        
        
        print("input error: \(input)")
        
        
//        let json = JSON(data_)
//
//        if let detail = json?["detail"].string {
//            print(detail)
//        } else if let error_ = error {
//            print(error_)
//        } else if let httpResponse_ = httpResponse {
//            print(httpResponse_)
//        }
        
        
        //        if httpResponse.statusCode >= 400 && httpResponse.statusCode != 401 {
        ////            print("httpResponse: \(httpResponse)")
        //            let detail = (json["detail"].string == nil) ? "\(httpResponse.statusCode)" : json["detail"].stringValue
        //            print("detail: \(detail)")
        //            let textError = ParseError.errors(detail: detail)
        //             игнорировать если не авторизован
        //            if httpResponse.statusCode == 401 {
        //                print("!!!! 401 !!!!!")
        //            } else {
        //                ParseError.showAlert(title: textError?.title, message: textError?.message)
        //            }
        //        }
    }
}
